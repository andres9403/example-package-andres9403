[build-system]
requires = ['hatchling']
build-backend = "hatchling.build"

[project]
name = "example_package_andres9403"
dynamic = ["version"]
dependencies = ["numpy"]
readme = "README.md"

[project.optional-dependencies]
test = ["pytest"]

[tool.hatch]
version

