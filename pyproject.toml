[build-system]
requires = ['hatchling']
build-backend = "hatchling.build"

[project]
name = "example_package_andres9403"
version = "0.1.0"
dependencies = ["numpy"]
readme = "README.md"

[project.optional-dependencies]
test = ["pytest"]